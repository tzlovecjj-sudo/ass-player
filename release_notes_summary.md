v1.2.0 — 简要说明（中文）

主要改动：
- 修复：修复浏览器原生全屏时顶部出现黑条的问题（调整 `static/css/fullscreen.css`）。
- 改进：增强 ASS 渲染器对样式的支持（描边、阴影、背景色、ScaleX/ScaleY、字间距、旋转、下划线、删除线等），并补充中文注释以便维护。 
- 测试与工具：新增按需本地验证运行器与验收截图脚本（用于本地人工验收辅助），并在 CI 中配置 Playwright E2E 测试（注意：测试代码与临时产物未包含在本发布包）。

验收信息：
- 单元测试已通过（本地记录）。
- 维护者已完成本地人工验收并确认通过。

说明：发布包仅包含运行所需的源码与静态资源，不包含 `tests/`、`tests_e2e/` 或 `artifacts/` 等测试临时目录。